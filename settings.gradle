pluginManagement {
    repositories {
        maven { url = "https://repo.papermc.io/repository/maven-public/" }
        gradlePluginPortal()
    }
}

rootProject.name = 'GloomLib'

file('modules').listFiles()?.findAll { it.isDirectory() }?.each {
    include "modules:${it.name}"
}